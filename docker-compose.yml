services:
  crowdsec-ui:
    build: ./ui
    container_name: crowdsec-ui
    restart: unless-stopped
    network_mode: host
    environment:
      - HELPER_URL=http://127.0.0.1:9099
      - HELPER_SECRET=${HELPER_SECRET}
